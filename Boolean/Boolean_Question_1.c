#include <stdio.h>
#include <stdbool.h>
int main (){
    bool isStudent = true;
    bool isGraduate = false;
    printf("Graduate: %d\n", isGraduate);
    printf("Student: %d\n", isStudent);
    return 0;
}
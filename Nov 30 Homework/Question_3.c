#include <stdio.h>
int main() {
    int age = 60;
        printf("first %d\n",age);
    if(age = 61)
        printf("second %d\n",age);
        printf("third %d\n",age);
}

//I predict that the code is going to be an error becuase in line 3 and 5, the equal signs are not double, which will cause problems.

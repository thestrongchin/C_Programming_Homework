#include <stdio.h>

int main(){
    int min;
    int max;

    int arr[6];
    for(int i = 0; i < 6; i++);
        printf("Please enter element %d: ", i + 1);
        scanf("%d", &arr[i]);
    }

    for(int)
    return 0;
}
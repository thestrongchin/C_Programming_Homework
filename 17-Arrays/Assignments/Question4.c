#include <stdio.h>
int main(){
    int arr[6];
    int min = arr[0]
    int max = 0;

    for(int i = 0; i < 6; i++){

        printf("Please enter element #%d: ", i + 1);
        scanf("%d", &arr[i]);
    }

    for(int n = 0; n < 6; n++){
        if(arr[0] )
    }

}
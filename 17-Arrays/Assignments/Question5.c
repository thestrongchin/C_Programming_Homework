#include <stdio.h>

int reverseArray(int arr[], int size){
    for(int i = 0; i < size; i++){
        n = arr[4], arr[3], arr[2], arr[1], arr[0];
        return arr[i];
    }


}

int main(){
    int arr[5] = {1, 2, 3, 4, 5};

    reverseArray(arr, 5);

    for (int i = 0; i < 5; i++){
        printf("Array: %d \n", arr[n]);
    }

    return 0;
}
